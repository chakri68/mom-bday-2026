/**
 * Kept for backwards compatibility. The source of truth for polaroid photos
 * is `src/content.json` (via `src/content.ts`). Edit the JSON to change them.
 */
export { POLAROID_IMAGES, type PolaroidImage } from "../content";
